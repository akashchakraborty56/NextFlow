import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const result = await prisma.nodeExecution.findFirst({ where: { nodeType: 'LLM', status: 'SUCCESS' }, orderBy: { completedAt: 'desc' }, select: { outputs: true, workflowNodeId: true, nodeType: true, status: true, completedAt: true, } }); if (result) { console.log('Node ID:', result.workflowNodeId); console.log('Type:', result.nodeType); console.log('Status:', result.status); console.log('Completed:', result.completedAt); console.log('--- OUTPUT ---'); console.log((result.outputs as any)?.generatedText || 'No generatedText found'); } else { console.log('No LLM execution found'); } } main() .catch(console.error) .finally(() => prisma.$disconnect());
